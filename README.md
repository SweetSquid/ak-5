## Lab5

### Usage

1. `make` to compile
2. `./main <keys>` to run

### Keys

```--help```, ```-h```.
```--version```, ```-v```.
```--add```, ```-a```.
```--list```, ```-l```.
```--list```, ```-l```.
```--file```, ```-f```.

